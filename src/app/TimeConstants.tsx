export { delayTime, fadeTime, loopTime };

const delayTime = {
    gameIntro: 900,
    newLevel: 1700,
};

const fadeTime = {
    title: 900,
    controlButtonActivate: 300,
};

const loopTime = {
    throttle: { default: 10 },
};
